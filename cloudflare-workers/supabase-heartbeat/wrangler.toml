name = "supabase-heartbeat"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# 定时触发器配置（每 5 天执行一次）
[triggers]
crons = ["0 0 */5 * *"]  # 每 5 天的午夜执行

# 如果需要，可以在这里设置环境变量
# [vars]
# SUPABASE_URL = "https://your-project.supabase.co"
# SUPABASE_ANON_KEY = "your-anon-key"
